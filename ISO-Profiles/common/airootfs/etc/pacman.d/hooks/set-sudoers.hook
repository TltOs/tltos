# remove from airootfs!
[Trigger]
Operation = Install
Type = Package
Target = nano

[Action]
Description = Changing Visudo file
When = PostTransaction
Depends = sh
Exec = sh -c "echo "Defaults editor=/usr/bin/nano" >> /etc/sudoers && rm -rf /boot/initramfs-linux-fallback.img"
